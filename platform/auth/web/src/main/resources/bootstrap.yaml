spring:
  application:
    name: auth-service

--- # local
spring:
  config:
    activate:
      on-profile: local
  cloud:
    config:
      enabled: false

--- # dev
spring:
  config:
    activate:
      on-profile: dev, stage, prod
  cloud:
    config:
      uri: http://127.0.0.1:8071
